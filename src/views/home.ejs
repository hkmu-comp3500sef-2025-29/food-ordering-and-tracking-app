<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Food Ordering And Tracking App</title>

    <style>
        body {
            margin: 0;
            background: url("/static/home.jpg") center/cover no-repeat fixed;
            color: #000;
            display: flex;
            min-height: 100vh;
            justify-content: center;
            align-items: center;
        }

        main {
            text-align: center;
            padding: 2rem;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            max-width: 420px;
        }

        h1 {
            margin-top: 0;
            font-size: 24px;
            font-weight: bold;
        }

        p {
            margin: 0.5rem 0 1.5rem;
            font-size: 1rem;
            color: #4b5563;
        }

        a {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            text-decoration: none;
            color: white;
            background: #ff8200;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 8px;
            transition: background 0.2s ease;
        }
    </style>
</head>

<body>
    <main>
        <h1><%= (navContext && navContext.restaurantName) || "Restaurant" %></h1>
        <p>You may order food and track it here.</p>
        <a href="/menu">Enter the menu</a>
    </main>
</body>
</html>
