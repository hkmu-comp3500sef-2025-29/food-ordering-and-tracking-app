<header>
    <div>
        <strong><%= (navContext && navContext.restaurantName) || "Restaurant" %></strong>
    </div>
    <% if (navContext && navContext.tableNumber) { %>
        <div>Table <%= navContext.tableNumber %></div>
    <% } %>
    <div>
        <button id="settings-btn">Settings</button>
    </div>
</header>
